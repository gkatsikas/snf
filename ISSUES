 * What if an element classifies from an already rewritten field
	EXAMPLE: IPRewrite->RadixLookup
	SOLUTIONS: IPRewrite makes the output of RadixLookup deterministic
		--> We should store an actual state of the field in the TrafficClass
